# Resumen de sesión — 2026-09-12 a 2026-09-14

Repo: `C:\Users\udetr\Desktop\ADRI 2`, rama `main`, remoto
`github.com/cozumeljewel/Cozumel`. Netlify publica la carpeta **`deploy/`**
en cada `git push` (no la raíz: las páginas `*-preview.html` de la raíz dan
404 en producción). **Todo cambio se hace en la raíz y se copia a `deploy/`**;
`python scripts/comprobar-deploy.py` verifica que estén sincronizadas.
Local: servidor `cozumel-local` (puerto 8123) en `.claude/launch.json`.

Todo lo de esta sesión está subido y en producción. Working tree limpio.

## 1. Infraestructura (cambió de cuenta)

- **Supabase nuevo**: proyecto **"Cozumel"**, ref `qesyjtqxbouodgldvbtq`,
  cuenta `cozumeljewel` (el antiguo "ADRI" `ddcrkglgdbasbxanbjkc` estaba en
  `eduatron@ucm.es`, sin acceso). Esquema completo aplicado (schema +
  migraciones v2–v11). `supabase-config.js` apunta al nuevo.
- Secretos ya configurados por el cliente: Vault `resend_api_key`; Edge
  Function secrets `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`,
  `RESEND_API_KEY`; Google OAuth (cliente nuevo en Google Cloud
  `cozumel-jewelry`); Auth → URL Configuration con `https://cozumeljewelry.es`.
- Edge Functions desplegadas: `crear-sesion-pago`, `webhook-stripe`,
  **`enviar-contacto`** (nueva: formulario de contacto → Resend a
  cozumeljewel@gmail.com, `verify_jwt=false`, honeypot, límite 3/10min por IP).
- Migración v11: el CHECK de `eventos` admite `compra_iniciada/completada`.
- La contraseña de la BD nueva se le pasó al cliente en el chat.

## 2. QA y copy

- CTA de personalizar se deshabilita si la pieza no tiene precio.
- Ocultas las etiquetas "imagen pendiente" (`.ph-tag`), quitado "Prototipo"
  e "Instagram pendiente" de Contacto.
- Voseo corregido en las 4 legales.
- FAQ de devoluciones alineado con Términos (política real = Términos).
- "Grabado a mano" → "grabado de alta calidad" en productos.html,
  productos.js, terminos.html y personalizar.html.
  **Queda sin cambiar (marcado para revisión) en historia.html**:
  "Cada grabado se realiza a mano, pieza a pieza…".

## 3. Sistema visual (referencia: hero de HOME)

- **Tipografía única**: `--font-serif` = Playfair Display (400 regular y
  400 italic), `--font-sans` = Jost (400/500/600). Playfair 700 solo en el
  logotipo en texto. Eliminados Petit Formal Script y pesos sobrantes del
  import de Google Fonts. Todos los titulares a 400.
- Colores del hero: `--hero-blanco #F6F1E8`, `--hero-arena #E7C98E`.
  Verde de titulares `--deep`.
- **Hero móvil** (solo móvil; escritorio conserva el centrado con firma de
  marca): titular a la izquierda, filete, descripción en 3 líneas
  "Joyas Personalizadas / Para regalar algo / Que Importa" (18px, blanco,
  600), CTA 86vw × 60px, foto `auto 108%` y encuadre 32% (2% en pantallas
  cortas) para no tapar la cara, velo localizado.
  El cliente rechazó oscurecer la foto y las sombras: el texto se resalta
  por peso/tamaño/color, no con efectos.
- **Cozumel Stories**: cabecera con serif 400 + "en la vida real" en
  cursiva champagne; quitadas las 4 líneas y el párrafo "Historias reales"
  (comentados en el HTML); fotos con hueco neutro `#E9E5DF`.
- **Colección**: intro corta (etiqueta sin cápsula, "La colección",
  "Un Pedacito de ti", frase); interludio resumido tras las piezas;
  "Los kits" como título h2 grande.
- **Nuestra historia**: todo centrado y sin justificar, sin onda, sin
  filetes, sin marca de agua; solo 2 grandes cursivas; "Lo que queremos
  transmitir" movido antes de "Más que una joya" y sin su foto (comentada).
- Botón volver arriba: visible en móvil, oculto en escritorio.

## 4. Pendiente / decisiones del cliente

- **Pulsera Dos Almas** no se personaliza → legalmente le aplicaría el
  desistimiento de 14 días; Términos dice "no devoluciones porque se hace a
  medida". Consultar gestoría.
- Confirmar "grabado a mano" de historia.html.
- El champagne `#E7C98E` sobre fondo claro se lee poco ("en la vida real",
  "de ti" en Colección). Propuesto usar `--gold-deep`; sin decidir.
- Precios siguen a 1 € de prueba; Stripe en modo test.
- Faltan todas las fotos reales (Stories, Historia, productos).
- Orden de meses del SKU con EMANCO sin confirmar.
- Quizá borrar `hero-opciones.html` (página interna de comparación).
